[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "meio_optimizer"
version = "0.1.0"
description = "Multi-Echelon Inventory Optimization engine"
requires-python = ">=3.9"

[tool.maturin]
module-name = "meio_optimizer"
features = ["pyo3/extension-module"]
